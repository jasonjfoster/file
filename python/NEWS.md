# file

## Version 0.1.2

* Added support for a list of form types in the `entry_form` and `exit_form` arguments of the `create_tenures` method (e.g., removals are filed by the issuer ("25") or the exchange ("25-NSE"))

## Version 0.1.0
